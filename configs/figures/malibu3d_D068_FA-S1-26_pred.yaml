# MALIBU3D ROI D068_FA-S1-26 — auto-generated by scripts/prepare_rois.py
# 1024 x 512 m, 14905038 points, 0.187 m spacing.
# Camera is the malibu3d_D075_UU-S1-4 pose scaled x2.00 for this ROI's extent —
# a starting point, not a tuned view. Refine it in the GUI and pull it back with
# scripts/scene_to_config.py.
extends: ../malibu3d.yaml

data:
  path: data/malibu3d/send_29_07_v2/blender_export/predictions/D068_FA-S1-26/D068_FA-S1-26.ply.gz
  voxel: 0.498
  palette_overrides:
    - configs/palettes/malibu3d_extra.json
    - configs/palettes/malibu3d_predictions.json
  subsample: 8000000   # of 14905038; voxel already scaled by sqrt(n_full/n_kept)

# Camera recovered 2026-08-26 -- APPROXIMATE, unlike the other two.
# /tmp/D068_FA-S1-26_pred_622996_autosave.blend was written at 17:03, two minutes
# AFTER the last render, and the camera had been nudged in between: the autosave
# pose scores only 0.879 silhouette IoU against forest_{gt,pred}.png. Refining the
# tilt (+2.50 deg about the camera's X axis) raises that to 0.942; the residual is a
# small translation this search did not chase. Expect a slightly different framing
# from the 2026-08-25 renders -- nudge in the GUI and pull back with scene_to_config.
# Note this tile used a LONGER lens than the others: fov_x 51.03, not 73.
camera:
  translation: [41.469967, -667.426086, 502.097412]
  quaternion: [0.8683585, 0.4950545, -0.0088469, -0.0282196]
  fov_x_deg: 51.0277
  near: 0.1
  far: 4095

graphs:
  items:
    - path: data/malibu3d/send_29_07_v2/blender_export/predictions/D068_FA-S1-26/D068_FA-S1-26_ROADS_graph.gpkg
      name: roads_gt
      color: [0.3, 0.55, 1.0]
    - path: data/malibu3d/send_29_07_v2/blender_export/predictions/D068_FA-S1-26/D068_FA-S1-26_ROADS_pred_graph.gpkg
      name: roads_pred
      color: [1.0, 0.42, 0.2]
